use mem map passed by grub to manage initial kmallocs rather than the placement_address

loading applications
	- libc support (stdio, main() etc)

shell

code layout for multiple usermode apps

memory
